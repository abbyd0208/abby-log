@AGENTS.md

# 寫作慣例（abby-log）

> 本節從既有 `src/content/blog/*.mdx` 與三場改稿對話歸納而來，供 AI 協助起草／改稿時遵守。
> **這是「還原作者既有風格」的規格，不是通用寫作建議**——與一般 SEO／內容行銷模板衝突時，以本節為準。
>
> **要動筆寫一篇新文章之前，先讀 `writing/WRITING-PLAYBOOK.md`**——那份有結構樣板（實驗紀錄型／方法論型）、素材準備規範、改稿與語氣迭代規則、Medium 平台坑清單。本節只管句法層。

## 讀者

**正在學 vibe coding、還在找工作流的 UI/UX 設計師。** 可信度來源是「這是我真的做過的事」——具體性是唯一護城河，不要為了通用而抽掉。

## 發布流程

**Medium 先發 → 回收進 blog。** 所以每篇 frontmatter 都有 `canonical` 指回 Medium 原文。

原稿與素材留在 `writing/`：`writing/seeds/<slug>.md`（素材袋）、`writing/archive/<slug>/`（發布前原稿與 HTML 閱讀版）。這兩個目錄不會被 `getAllPosts()` 讀到，只有 `src/content/blog/*.mdx` 才是文章。

## Frontmatter 規格

```yaml
---
title: "..."        # 常用「結論：理由」或「否定式斷言」句型，見下方
date: "YYYY-MM-DD"
summary: "..."      # 通常直接取內文第一段，或用一句情境提問
tags: [...]         # 見下方標籤池
canonical: "https://abby-yl-ting.medium.com/..."   # Medium 原文（URL-encoded 中文標題）
---
```

**標籤池**（真相在 `src/lib/site.ts` 的 `tagGroups`，沿用、不要自創同義標籤；新增標籤要同步加進 `tagGroups`，否則顏色退回預設藍）：

- 主題：`ai-workflow`｜`design-thinking`｜`tools`｜`career`｜`lifestyle`
- 類型：`tutorial`｜`reflection`｜`case-study`｜`experiment`｜`mindset`
- 工具：`claude`｜`figma`｜`video`

## 文章結構

- **標題階層從 `###` 起跳**，不用 `##`（`##` 只在技術示範類文章出現）。需要更細一層才用 `####`
- **開場不寫「前言」二字**，直接進情境。兩種開法：
  - 情境提問：「如果你在 SaaS 公司工作過，你一定知道這個場景：⋯⋯」
  - 共感提問：「在設計流程即將交付的那一刻，你是否也曾出現過這樣的感覺？」
- **中段慣用編號小節**拆解問題或步驟：`### 1. …` `### 2. …`，或 `### Step 1：A → B`（步驟型用箭頭表示輸入→產出）
- **結尾固定是「給同處境者的建議」**，用 1/2/3 編號列出，每點以粗體短句起頭再展開
- 最後一句是**行動呼籲**：拍手、留言、follow，語氣輕鬆（「以上感謝收看～」）
- **文章最後固定放這段簽名，一字不改**（行動呼籲若要放，放在簽名之前）：

  > 與AI 的工作流就是一個不斷優化與學習的過程，同一個領域的人，面對這種靈活的工具，使用的方式也不盡相同。
  >
  > 透過各種不同實驗，享受各種新發現、讓工作流程更順暢，讓自己不要陷入過度的資訊焦慮，共勉之！

## 語氣與人稱

- **第一人稱、口語、真實**。允許「老實說」「我覺得」「心理落差會比較大」這類主觀表述
- **不藏失敗**。文章價值來自「什麼順、什麼卡」的對照——既有文章有專門段落寫這個，要保留
- **不誇大 AI**。既有結論多為「AI 不會取代 X，但能讓沒有 X 的團隊跨過門檻」這種有邊界的說法。**不要寫成 AI 萬能**
- 中英混用是常態（Prototype、Flow、Component、motion graphic 等直接用原文），**不要為了統一而全部翻成中文**
- **不要過度自信、不要中二感。** 目標是「跟同事分享成果報告」的口吻。曾被逐處改掉的字：終於／崩潰／靈魂／哲學／最關鍵／最痛／打臉／敢。改成平視語氣：老實說、蠻有意思、大概會有點用、給同行參考
- **降低門檻的自嘲要留**（「我並沒有用什麼厲害的技術，我就只是把他們都可視化了」），不要在改稿時被「專業化」掉
- **戲劇化的獨立短句要節制**。單獨成段的「錯了。」（模仿英文的 "Wrong."）發布時被拿掉了——中文讀起來太硬

## 標題

**判準只有一條：只有讀過這篇的人才寫得出來的標題。** 中文技術寫作的萬用詞（「踩過的坑」「難的不是 X」）沒有資訊量，換一篇文章也能用。

好的做法是引用文章裡最尖銳的那句真話——「我做的 AI 設計工作流叫『全自動』——它最需要的其實是停下來」、「每次對話，AI 都是第一天上班」。副標負責認領讀者與界定範圍。

## 慣用的修辭

- **粗體用在「可獨立成立的一句話結論」**，不是用來標關鍵詞
  - 例：「**流程能跑，不等於體驗成立。**」「**選對的工具做對的事，不用堅持一個工具打天下。**」
- **對比句式是主要武器**：「檢查了執行結果（execution），卻忽略了設計意圖（intention）」
- **blockquote 放金句或引用**，不放補充說明
- 適量 emoji（🔗 1️⃣ 👉），集中在號召讀者行動的段落，內文不灑

## 圖片

- 路徑：`/images/{文章 slug}/01.png`（兩位數編號）
- **圖片下方用斜體寫圖說**，語氣可以隨性：「*不知道配什麼圖才好，讓GPT產了一個…*」
- 純示意圖也照放，不必每張都有資訊量

## 寫「我如何用 AI 工作」這類文章時

作者的四段結構：**做了什麼 → 我的想法 → prompt 怎麼下 → 產出長怎樣**。

其中**「prompt 怎麼下」最容易被寫壞**——只放最後那版漂亮的 prompt，讀者照抄不會有同樣結果。務必保留：

1. 第一次下的 prompt 是什麼
2. 為什麼不行
3. 改了哪裡才 work

既有文章的實例：「這邊的進場效果要多一點速度感」→ 說明 AI 會去調 spring 的 stiffness 和 damping。**用感受描述 + 說明它實際改了什麼**，這種寫法要延續。

## 改稿時的四條紀律

1. **同一組結論全篇只出現兩次**——開頭 TL;DR ＋ 結尾可複製清單，頭尾呼應就夠。踩過一次：同一組原則出現在三個區塊，「把確認點放在便宜的地方」講了三遍，讀者讀到第二次就開始滑
2. **最原創的那節不要排在 60% 的位置**；最誠實、最容易被轉發的段落（承認做錯）不要縮在 blockquote 當附註，升格成正式一節
3. **名詞解釋不要擋在最前面**。讀者剛被開場鉤住就被定義冷卻是勸退設計——拆成首次出現時的括號註解，或挪到文末附錄
4. **數字要有佐證才寫**。「三週沒看」寫進標題後才發現實際是九天。天數／次數／字數都要對得上 log 或 commit，查不到就標 `TBD` 問作者，不要自己推

## 不要做的事

- 不要加「前言」「結語」「總結」當標題（作者用內容自然收尾）
- 不要把中英混用「修正」成純中文
- 不要刪掉承認失敗或不確定的句子
- 不要生成沒有實際體驗支撐的建議——這個 blog 的價值全來自第一手實作
- 不要把「做過才發現要補」寫成「我早就設計好了，來驗證一下」——後者是不準確的敘事，而前者才是文章最有價值的地方
- 不要替作者宣稱查不到的事（「這些領悟回灌進工具裡」查完 git 發現沒有）
- 不要放產品截圖、客戶名、Jira／PR 編號、Figma 檔名、內部元件名、AI 模型型號——泛化成「同類產品」「設定頁」
